 /** @type {import('next').NextConfig} */
const nextConfig = {
  serverExternalPackages: ['sharp'],
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'i.pravatar.cc',
      },
      {
        protocol: 'https',
        hostname: 'images.unsplash.com',
      }
    ],
  },
  typescript: {
    ignoreBuildErrors: true,
  },
  webpack: (config, { isServer }) => {
    if (isServer) {
      // Exclude large dependencies from serverless functions
      config.externals = [
        ...config.externals,
        'groq-sdk',
        '@ai-sdk/groq',
        '@ai-sdk/openai',
        'ai',
        'ollama',
        'nodemailer',
        'jspdf',
        'jspdf-autotable',
        'sharp',
        'postgres',
        'csv-parse',
        'effect',
        'framer-motion',
        '@mdxeditor/editor'
      ]
    }
    return config
  },
  turbopack: {
    // Turbopack configuration to exclude large dependencies
    rules: [
      {
        test: /\.(node)$/,
        use: 'node-loader',
      },
    ],
  }
}

module.exports = nextConfig
